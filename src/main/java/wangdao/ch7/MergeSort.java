package wangdao.ch7;

import java.util.ArrayList;

public class MergeSort {
    // 辅助数组
    static ArrayList assistArr;

    public static void initAssistArray(int arrSize) {
        assistArr = new ArrayList(arrSize);
    }

    public static void merge(ArrayList arr, int low, int mid, int high) {
        initAssistArray(arr.size());
        for (int k = low; k <= high; k++) {


        }
    }
}
